# Encoder configs (OmegaConf → `python -m encoder.train`)

| File | Dataset |
|------|---------|
| `fixed_fovea.yaml` | THINGS-EEG |
| `alljoined_ubp.yaml` | Alljoined-1.6M |

Predictor (image→EEG) hyperparameters live in `scripts/train_*.sh` / `python -m predictor.train` CLI — no yaml here.
